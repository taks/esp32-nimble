[package]
name = "esp32-nimble"
version = "0.0.1"
authors = ["taks <857tn859@gmail.com>"]
edition = "2021"
resolver = "2"
repository = "https://github.com/taks/esp32-nimble"
license = "MIT"

[profile.release]
opt-level = "s"

[profile.dev]
debug = true
opt-level = "z"

[features]
pio = ["esp-idf-sys/pio"]

[dependencies]
log = { version = "0.4", default-features = false }
esp-idf-sys = { version = "0.31.11", default-features = false }
esp-idf-hal = { version = "0.39.2", default-features = false, features = ["critical-section", "embassy-sync"] }
esp-idf-svc = { version = "0.43.1", default-features = false, features = ["alloc"] }
embedded-svc = { version = "0.23.0", default-features = false }

critical-section = { version = "1.1.1" }
once_cell = { version = "1.16.0", default-features = false, features = ["critical-section"] }
embassy-sync = { version = "0.1.0" }
bitflags = { version = "2.0.0-rc.1" }

[dev-dependencies]
esp-idf-sys = { version = "0.31.11", default-features = false, features = ["binstart", "alloc_handler"] }
esp-idf-hal = { version = "0.39.2", default-features = false, features = ["critical-section", "edge-executor", "embassy-sync"] }
esp-idf-svc = { version = "0.43.1", default-features = false, features = ["alloc", "embassy-time-driver", "embassy-time-isr-queue"] }
embedded-hal = { version = "=1.0.0-alpha.9", default-features = false }
thingbuf = { version = "0.1.3", default-features = false, features = ["alloc"] }
embassy-time = { version = "0.1", features = ["tick-hz-1_000_000"] }

[build-dependencies]
embuild = "0.30.4"
anyhow = "1"
